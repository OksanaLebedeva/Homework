# Домашнее задание:
1. В сообщении из 1000 символов, используется алфавит из 64 символов. Каков объем информации в этом сообщении?

Ответ: a) 2^i = 64, i = 6; b) 6 * 1000 = 6000 бит = 750 байт 

2. Сообщение из 1024 символов занимает 1/128 мегабайта. Сколько символов в исходном алфавите?

Ответ: a) 1/128 Мегабайта = 1024 килобайт : 128 = 8 килобайт = 8 * 1024 = 8192 байт = 65536 бит; b) 65536 / 1024 = 64 бит на символ; c) 2^64 = 1,8446744 ? 10^19 (примерно)

3. Печатая текст в кодировке UTF-16 пользователь успевает набрать 128 символов в минуту. Сколько килобайт информации будет внесено в документ в течение пяти минут?

Ответ: a) 16 бит / 8 = 2 байт на символ; b) 128 * 2 * 5 / 1024 = 1.25 килобайта

4. Перевести 28 в троичную систему счисления

 Ответ: 1001
* 28/3 = 9 ост. 1
* 9/3 = 3 ост. 0
* 3/3 = 1 ост. 0
* 1/3 = 0 ост. 1

5. RGB-картинка занимает ~72 килобайта, какой у неё размер в пикселях?

Ответ: a) 256 оттенков одного цвета => 1 байт на 1 пиксель одного цвета => 3 байта на 1 пиксель трех цветов; b) 72kb / 3 байта = 72 * 1024 / 3 = 24 * 1024 пикселей = 2^2 * 6 * 2^10; с) кв. корень = 2^6 * sqrt(6) = 156. То есть картинка 156 на 156 примерно. 

6. * Описать блок-схему перевода десятичного представления числа в двоичное.

Ответ: start -> 1) число делим на 2 -> 2) получаем результат и остаток -> 3) запоминаем остаток -> 4) если результат шага 2 больше 0, идем в пункт 2 -> 5) иначе выходим из цикла и записываем остатки из пункта 3 в обратном порядке -> 6) stop